//uded for grabbing only below data in hnapi.js
// ({ id, url, by, title, time }={ this braces is useed for empty if any other variable comes })


export const selectFields = ({ id, url, by, title, time }={})  =>( {
  id,
   url,
    by,
     title,
      time,
})
